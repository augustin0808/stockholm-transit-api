server:
  http_listen_port: 9080

positions:
  filename: C:/promtail/positions.yaml

clients:
  - url: http://localhost:3100/loki/api/v1/push

scrape_configs:
  - job_name: fastapi_logs
    static_configs:
      - targets:
          - localhost
        labels:
          job: fastapi
          __path__: C:/Users/augus/stockholm_transit_api/app.log
